#include <cstring>
#include <iostream>
#include <queue>
#include <utility>

using namespace std;

#define endl '\n'
#define MAX 100001

int students[MAX];
bool visit[MAX];

/**
 * @see [텀 프로젝트](https://www.acmicpc.net/problem/9466)
 */
int main() {
    ios::sync_with_stdio(false), cin.tie(NULL), cout.tie(NULL);
    int T;
    cin >> T;
    while (T--) {
        // TODO: code here
    }
}