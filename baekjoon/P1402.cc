#include <iostream>

#define fastio cin.tie(0)->sync_with_stdio(0)
#define endl '\n'

using namespace std;

/**
 * @see [아무래도이문제는A번난이도인것같다](https://www.acmicpc.net/problem/1402)
 */
int main() {
    fastio;
    int T;
    cin >> T;
    while (T--) {
        long long A, B;
        cin >> A >> B;
        cout << "yes" << endl;
    }

    return 0;
}