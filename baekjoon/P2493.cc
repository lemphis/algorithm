#include <iostream>

#define endl '\n'

using std::cin;
using std::cout;
using std::ios;

/**
 * @see [탑](https://www.acmicpc.net/problem/2493)
 */
int main() {
    ios::sync_with_stdio(false), cin.tie(NULL), cout.tie(NULL);
    int N;
    cin >> N;
    int heights[N];
    for (int i = 0; i < N; ++i) {
        cin >> heights[i];
    }
}